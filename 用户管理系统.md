# Java 用户管理系统 (UMS) 项目设计文档

**版本**：V1.0  
**状态**：规划中  
**项目经理**：AI 助手  
**目标**：构建一个轻量级、易扩展、安全的用户管理后端服务。

---

## 1. 项目概述 (Project Overview)
本系统旨在提供一个标准的用户管理核心功能，包括用户的注册、登录、信息查询、修改及删除（CRUD）。系统采用前后端分离架构，本期只负责后端 API 开发，不包含前端页面。

## 2. 技术选型 (Technology Stack)
*   **核心语言**：Java 17 (LTS 版本)
*   **开发框架**：Spring Boot 3.x (Web, Validation)
*   **持久层**：Spring Data JPA + Hibernate
*   **数据库**：MySQL 8.0 (生产环境) / H2 (开发测试环境)
*   **安全认证**：Spring Security + JWT (JSON Web Token)
*   **API 文档**：SpringDoc OpenAPI (Swagger UI)
*   **构建工具**：Maven
*   **工具库**：Lombok, MapStruct

## 3. 功能需求 (Functional Requirements)

### 3.1 核心模块
1.  **用户认证模块**
    *   用户注册（用户名唯一性校验、密码加密存储）
    *   用户登录（颁发 JWT Token）
    *   用户登出（客户端销毁 Token，服务端可选黑名单机制）
2.  **用户管理模块 (CRUD)**
    *   创建用户（管理员权限）
    *   根据 ID 查询用户详情
    *   分页查询用户列表（支持按用户名模糊搜索）
    *   更新用户信息（昵称、邮箱等）
    *   删除用户（逻辑删除或物理删除）
3.  **个人中心模块**
    *   获取当前登录用户信息
    *   修改个人密码
    *   修改个人资料

## 4. 数据库设计 (Database Schema)

**表名：`sys_user`**

| 字段名        | 数据类型     | 约束               | 描述                  |
| :------------ | :----------- | :----------------- | :-------------------- |
| `id`          | BIGINT       | PK, Auto Increment | 用户唯一标识          |
| `username`    | VARCHAR(50)  | Unique, Not Null   | 登录用户名            |
| `password`    | VARCHAR(255) | Not Null           | BCrypt 加密后的密码   |
| `nickname`    | VARCHAR(50)  |                    | 用户昵称              |
| `email`       | VARCHAR(100) | Unique             | 邮箱地址              |
| `phone`       | VARCHAR(20)  |                    | 手机号                |
| `status`      | TINYINT      | Default 1          | 状态 (1:正常, 0:禁用) |
| `create_time` | DATETIME     | Not Null           | 创建时间              |
| `update_time` | DATETIME     |                    | 更新时间              |

## 5. API 接口设计 (API Specification)

基础路径：`/api/v1`

### 5.1 认证接口
*   **POST /auth/register**：用户注册
*   **POST /auth/login**：用户登录

### 5.2 用户管理接口 (需 Bearer Token)
*   **GET /users/{id}**：获取指定用户信息
*   **GET /users**：分页查询用户列表
*   **PUT /users/{id}**：更新用户信息
*   **DELETE /users/{id}**：删除用户 (仅管理员)
*   **GET /users/me**：获取当前登录用户信息
*   **PUT /users/me/password**：修改当前用户密码

## 6. 系统架构 (Architecture)
采用经典的三层架构：
1.  **Controller 层**：处理 HTTP 请求，参数校验。
2.  **Service 层**：业务逻辑核心，事务控制。
3.  **Repository (DAO) 层**：与数据库交互，使用 JPA 接口。
4.  **Entity 层**：数据库实体映射。
5.  **DTO/VO 层**：数据传输对象。

## 7. 安全设计 (Security)
*   **密码安全**：使用 `BCryptPasswordEncoder` 进行单向哈希加密。
*   **认证机制**：无状态 JWT。
*   **权限控制**：`ROLE_USER` 和 `ROLE_ADMIN`。
*   **参数校验**：使用 `@Valid` 注解防止非法参数注入。

## 8. 项目结构 (Project Structure)
```text
src/main/java/com/example/ums/
├── config/          # 配置类
├── controller/      # API 接口
├── dto/             # 请求/响应对象
├── entity/          # 数据库实体
├── exception/       # 全局异常处理
├── repository/      # 数据访问层
├── service/         # 业务逻辑层
├── util/            # 工具类
└── UmsApplication.java